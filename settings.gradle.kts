pluginManagement {
    repositories {
        maven {
            name = "Spring Milestones"
            url = uri("https://repo.spring.io/milestone")
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "demo"
